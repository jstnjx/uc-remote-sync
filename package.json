{
  "name": "uc-remote-sync",
  "version": "0.7.2",
  "description": "Synchronize activities, profiles, pages, entities, states, and commands from a primary Unfolded Circle Remote to satellite remotes, with Dock 3 access through authenticated tunnels.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node src/driver.js",
    "check": "node tools/check-source.js",
    "build:embedded": "node tools/build-embedded.js",
    "package": "npm run check && npm run build:embedded",
    "test": "node --test",
    "ci": "npm run check && npm test"
  },
  "license": "MIT"
}
